- hosts: Test
  gather_facts: no
  tasks:
    - name: Kill experiment by PID
      shell: |
        if [ -f ~/experiment.pid ]; then
          kill -9 "$(cat ~/experiment.pid)" || true
          rm -f ~/experiment.pid
        fi
      args:
        executable: /bin/bash
